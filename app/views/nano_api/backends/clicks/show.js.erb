<% if 'get' == @click_params[:method] and @click_params[:params].blank? %>
	setTimeout(function(){
  	window.location.href = '<%= raw @click_params[:url] %>';
	}, timeout)
<% else %>
	setTimeout(function(){
		document.write('<%= j render 'form', form: @click_params %>');
		document.getElementById('nano_api_click_form').submit();
	}, timeout);
<% end %>
